<Window x:Class="ReifeschrankTracker.Views.GewichtEintragenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gewicht eintragen"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Zeitpunkt" VerticalAlignment="Center" Margin="0,4"/>
            <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding Zeitpunkt}" Margin="4,4"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Gewicht (g) *" VerticalAlignment="Center" Margin="0,4"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding GewichtText, UpdateSourceTrigger=PropertyChanged}" Margin="4,4"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Notiz" VerticalAlignment="Top" Margin="0,4"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Notiz, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="4,4"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="{Binding Fehler}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,8"
                       Visibility="{Binding Fehler, Converter={StaticResource StringToVisibility}}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Speichern" Width="90" Margin="0,0,8,0" Click="Speichern_Click" IsDefault="True"/>
                <Button Content="Abbrechen" Width="90" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
